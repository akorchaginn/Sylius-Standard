image_admin_slide:
    prefix: /admin
    resource: |
        alias: image.slide
        section: admin
        templates: SyliusAdminBundle:Crud
        redirect: update
        grid: image_admin
        vars:
            index:
                icon: 'icon newspaper'
    type: sylius.resource