<a class="menu-catalog__title ui blue submit button" href="#">Открыть каталог</a>
<div class="catalog-menu__wrap menu-catalog-hide">
    <div class="ui fluid vertical menu">
        <ul id="menu-catalog" class="list-unstyled">
        {% for product in resources %}
            <li class="item">    <a href="{{ path('sylius_shop_product_show', {'slug': product.slug, '_locale': product.translation.locale}) }}" class="header sylius-product-name">{{ product.name }}</a> </li>
        {% endfor %}
        </ul>
    </div>
</div>



